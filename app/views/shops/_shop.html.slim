| <li id="shop-
= shop.id
| "> 
= link_to gravatar_for(shop.user, size: 50), shop.user
span.name
  = link_to shop.name, "#"
br
span.address
  = link_to shop.address, "#"
span.description
  = shop.description
span.reommended_points
  | おすすめポイント：
  = shop.recommended_points
br
span.rating
  | 人気度：
  = shop.rating
br
span.web_page
  = link_to "WEBページ", shop.web_page, target: :_blank
span.timestamp
  = time_ago_in_words(shop.created_at)
  | 前に投稿 